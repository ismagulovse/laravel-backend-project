<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | Секретный ключ webhook'а
    |--------------------------------------------------------------------------
    |
    | Ключ, который должен прийти в теле запроса на /hooks/git.
    | Сравнивается с присланным значением регистрозависимо. Берётся из .env.
    |
    */
    'webhook_secret' => env('GIT_WEBHOOK_SECRET'),

    /*
    |--------------------------------------------------------------------------
    | Ветка по умолчанию для деплоя
    |--------------------------------------------------------------------------
    |
    | Ветка, на которую переключается и которую подтягивает деплой.
    | Не хардкодится в сервисе — берётся отсюда (Open/Closed).
    |
    */
    'default_branch' => env('GIT_DEFAULT_BRANCH', 'main'),

    /*
    |--------------------------------------------------------------------------
    | Путь к git-репозиторию
    |--------------------------------------------------------------------------
    |
    | Директория, в которой выполняются git-команды. По умолчанию — корень проекта.
    |
    */
    'repository_path' => env('GIT_REPOSITORY_PATH', base_path()),

    /*
    |--------------------------------------------------------------------------
    | Таймаут блокировки деплоя (секунды)
    |--------------------------------------------------------------------------
    |
    | Максимальное время удержания блокировки. По истечении блокировка снимается
    | автоматически, чтобы упавший процесс не заблокировал деплой навсегда.
    |
    */
    'lock_timeout' => (int) env('GIT_DEPLOY_LOCK_TIMEOUT', 300),

];
